exports.loginlightblue = async (session, models, vars) => {
    models.loginlightblue = vars.page;
    await session.screen('loginlightblue');
};
exports.loginlightblue2 = async (session, models, vars) => {
    models.loginlightblue = vars.page;
    await session.screen('loginlightblue');
};