exports.help = async (session, models, vars) => {
    await session.transform.powwowtraining60.update('homeiconcolorblocks', models.homeiconcolorblocks);
    await session.transform.powwowtraining60.action('homeiconcolorblocks', 'help');
};
exports.alerts = async (session, models, vars) => {
    await session.transform.powwowtraining60.update('homeiconcolorblocks', models.homeiconcolorblocks);
    await session.transform.powwowtraining60.action('homeiconcolorblocks', 'alerts');
};
exports.newIssue = async (session, models, vars) => {
    await session.transform.powwowtraining60.update('homeiconcolorblocks', models.homeiconcolorblocks);
    await session.transform.powwowtraining60.action('homeiconcolorblocks', 'newIssue');
};
exports.monitor = async (session, models, vars) => {
    await session.transform.powwowtraining60.update('homeiconcolorblocks', models.homeiconcolorblocks);
    await session.transform.powwowtraining60.action('homeiconcolorblocks', 'monitor');
};
exports.unassigned = async (session, models, vars) => {
    await session.transform.powwowtraining60.update('homeiconcolorblocks', models.homeiconcolorblocks);
    await session.transform.powwowtraining60.action('homeiconcolorblocks', 'unassigned');
};
exports.assigned = async (session, models, vars) => {
    await session.transform.powwowtraining60.update('homeiconcolorblocks', models.homeiconcolorblocks);
    await session.transform.powwowtraining60.action('homeiconcolorblocks', 'assigned');
};